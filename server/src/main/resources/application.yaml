server:
  port: 8080
#  servlet:
#    context-path: /posture-checker

spring:
  application:
    name: server
  datasource:
#    url: jdbc:mysql://localhost:3306/posture_checker_db?useSSL=false&serverTimezone=UTC
    url: jdbc:mysql://localhost:3306/posture_checker_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

spring:
  application:
    name: server
  datasource:
    #    url: jdbc:mysql://localhost:3306/posture_checker_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    url: {DB_URL}
    username: {DB_USERNAME}
    password: {DB_PASS}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
